{
  "scripts": {
    "build": "spago build",
    "test": "spago bundle",
    "publish": "spago publish"
  },
  "dependencies": {
    "rough-notation": "^0.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "spago": "next"
  }
}
